import { Bell, CheckCheck, MessageSquare, ShieldCheck } from "lucide-react"
import { useState } from "react"

const initialNotifications = [{ id: 1, icon: MessageSquare, title: "New message", text: "You have a new campus conversation.", time: "Just now", unread: true }, { id: 2, icon: ShieldCheck, title: "Stay safe", text: "Always verify ownership before returning an item.", time: "Today", unread: true }]
const Notifications = () => { const [items, setItems] = useState(initialNotifications); const markAll = () => setItems((current) => current.map((item) => ({ ...item, unread: false }))); return <div className="mx-auto max-w-2xl px-4 py-8 sm:px-6"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-400">Activity</p><h1 className="mt-1 text-3xl font-bold">Notifications</h1></div><button onClick={markAll} className="inline-flex items-center gap-1.5 text-sm font-semibold text-blue-400 hover:text-blue-300"><CheckCheck className="h-4 w-4" />Mark all read</button></div><div className="mt-7 overflow-hidden rounded-2xl border border-border bg-card">{items.length ? items.map((item) => { const Icon = item.icon; return <button key={item.id} onClick={() => setItems((current) => current.map((entry) => entry.id === item.id ? { ...entry, unread: false } : entry))} className={`flex w-full gap-4 border-b border-border p-4 text-left last:border-0 ${item.unread ? "bg-blue-500/5" : ""}`}><span className="rounded-xl bg-secondary p-2.5 text-blue-400"><Icon className="h-5 w-5" /></span><span className="min-w-0 flex-1"><span className="flex items-center gap-2 font-semibold">{item.title}{item.unread && <i className="h-2 w-2 rounded-full bg-blue-400" />}</span><span className="mt-1 block text-sm text-muted-foreground">{item.text}</span><span className="mt-2 block text-xs text-muted-foreground">{item.time}</span></span></button> }) : <div className="p-12 text-center text-muted-foreground"><Bell className="mx-auto h-8 w-8" /><p className="mt-3 text-sm">You’re all caught up.</p></div>}</div></div> }
export default Notifications
