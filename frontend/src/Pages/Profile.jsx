import { BadgeCheck, Mail, School, UserRound } from "lucide-react"
import { useAuthStore } from "../lib/authStore"

const Profile = () => { const user = useAuthStore((state) => state.user); if (!user) return null; return <div className="mx-auto max-w-3xl px-4 py-8 sm:px-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-400">Account</p><h1 className="mt-1 text-3xl font-bold">My profile</h1><section className="mt-7 overflow-hidden rounded-2xl border border-border bg-card"><div className="h-28 bg-[linear-gradient(120deg,rgba(59,130,246,.4),rgba(20,184,166,.18))]" /><div className="px-6 pb-7"><img src={user.profilePic || "https://avatar.iran.liara.run/public"} alt="" className="-mt-12 h-24 w-24 rounded-2xl border-4 border-card object-cover" /><div className="mt-3 flex items-center gap-2"><h2 className="text-2xl font-bold">{user.fullname}</h2>{user.isVerified && <BadgeCheck className="h-5 w-5 text-blue-400" />}</div><p className="mt-1 text-sm text-muted-foreground">{user.faculty || "UniFind community member"}</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><Info icon={Mail} label="Email" value={user.email} /><Info icon={School} label="Academic level" value={user.level ? `Level ${user.level}` : "Not set"} /><Info icon={UserRound} label="Account role" value={user.role || "customer"} /></div></div></section></div> }
const Info = ({ icon: Icon, label, value }) => <div className="rounded-xl border border-border bg-secondary/40 p-4"><Icon className="h-4 w-4 text-blue-400" /><p className="mt-2 text-xs text-muted-foreground">{label}</p><p className="mt-0.5 break-all text-sm font-semibold capitalize">{value}</p></div>
export default Profile
